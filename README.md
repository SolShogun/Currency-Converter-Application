# CurrencyConverter

Simple two-view iOS app using MVC.
Open in Xcode, create a Single View App named CurrencyConverter, then replace the generated files with the included Swift files and Main.storyboard.
